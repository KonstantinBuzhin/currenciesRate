import { EmailValidator } from "@angular/forms";

export class User {
    id!: number;
    name!: string;
    address!: string;
    phone!: string;
    email!: string;
    password!: string;
    constructor(){}
    
}
